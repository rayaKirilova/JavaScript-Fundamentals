function object([prop1 , value1, prop2, value2, prop3, value3]) {

    console.log(`{${prop1}: '${value1}', ${prop2}: '${value2}', ${prop3}: '${value3}'}`) ;
}

object(['name', 'Pesho', 'age', '23', 'gender', 'male']) ;